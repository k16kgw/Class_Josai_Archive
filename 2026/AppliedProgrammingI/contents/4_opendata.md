# 第4回　オープンデータの取得と整理

### 前回の復習

- Git の履歴は単なる直線ではなく，分岐をもつグラフとして理解できる．
- ブランチは履歴上の特定のコミットを指す参照であり，`HEAD` は現在の作業位置を示す．
- マージは分岐した作業系列を統合する操作である．
- コンフリクトは同じ箇所に対して両立しない変更が加えられた場合などに発生する．
- データ分析では，安定版と試行版を分けることで，再現可能性と探索性を両立できる．
<!-- 
前回までは，主にコードや文書の変更履歴を Git で管理する方法を学んだ．
しかし，データ分析の再現可能性はコードだけでは成立しない．同じプログラムを実行しても，入力データが異なれば結果も変わる．
したがって，分析に用いるデータについても，どこから，いつ，どの条件で取得したかを記録する必要がある．
 -->

### 到達目標

分析の入力となるデータをどのように取得，整理，記録するかを扱う．  
特に，オープンデータを出典，ライセンス，形式，更新性をもつ情報資源として理解することを目標とする．

- オープンデータの定義と意義を説明できる．
- データの出典，ライセンス，更新頻度，取得時点，形式を確認できる．
- CSV，JSON，Excel，PDF などの代表的な形式の違いを説明できる．
- 生データと加工データを分けて保存する理由を説明できる．
- Git 管理の下で，データ取得過程と整理過程を再現可能な形で残せる．

### 準備

````{note} 演習0
本講義で使用するフォルダ `/User/<ユーザ名>/applied_programming_i/` 内に，本日使用するフォルダ `4` を作成し，次の `README.md` ファイルと `.gitignore` ファイルを作成した上で Git の初期化を行うこと．
※ ターミナル・VSCodeのどちらで実施しても構わない．

README.md
```markdown
# 応用プログラミングI 第4回

- 氏名：<氏名>
- 学籍番号：<学籍番号>

## 今日の目標

オープンデータの取得条件を確認し，データ本体と取得記録を整理して保存する．
```

.gitignore
```text
.DS_Store
*.swp
*.swo
*~
.vscode/
data/raw
```

**手順**
```{dropdown} A. ターミナルで実施する場合
1. ターミナルを起動する．
2. フォルダに移動する．`$ cd /User/<ユーザ名>/applied_programming_i`
3. フォルダ`4`を作成する．`$ mkdir 4`
3. フォルダ`4`に移動する．`$ cd 4`
4. 初期化する．`$ git init`
5. README.mdを作成する．`$ vim README.md`でファイルの中身を記入する．
6. 全てのファイルをステージ領域に入れる．`$ git add .`
7. ステージ領域のファイルをコミットする．`$ git commit -m "first commit"`
```

```{dropdown} B. VSCodeで実施する場合
1. VSCodeを起動する．
2. メニューバー＞ファイル＞フォルダーを開く
3. `/User/<ユーザ名>/applied_programming_i`に移動して「新しいフォルダを作成」をクリック
4. `4`フォルダを作成して選択
5. 左のgitタブ＞「リポジトリを初期化する」を選択
6. README.mdを作成する．（「新しいファイル」のアイコンをクリックし，ファイル名を指定してファイルの中身を記入する）
7. 全てのファイルをステージ領域に入れる．（`+`をクリック）
8. ステージ領域のファイルをコミットする．（メッセージを記入して「コミット」をクリック）
```
````
<!-- 
1. ターミナルを起動
   1. フォルダ移動（`$ cd /User/<ユーザ名>/applied_programming_i/`）
   2. フォルダ `4` を作成（`$ mkdir 4`）
2. VSCodeを起動
   1. フォルダ `/User/<ユーザ名>/applied_programming_i/4` を開く
   2. `README.md` ファイルを作成する
3. Gitの初期化を行う
   1. 左のGitタブを開き「リポジトリを初期化する」を選択する
4. 最初のコミットを行い初期状態を記録する
   1. 全てのファイルをステージング領域に上げる
   2. コミットする（メッセージを忘れずにつけること）
 -->

---

## データを見ることの意義

```{note} 演習（番外編）
**【問１】** 世界の人口のうち、極度の貧困†にある人の割合は、1990年から2015年までの25年間でどう変わったでしょう？  
A. 約2倍になった  
B. あまり変わっていない  
C. 半分になった

**【問２】** 2015年時点で世界中の1歳児の中で、何らかの病気に対して予防接種を受けている子供はどのくらいいるでしょう？  
A. 20%  
B. 50%  
C. 80%

参考：ハンス・ロスリング他「FACTFULNESS」日経BP社，(2019) pp.8–17．を参考に直近のデータを元に改変．

極度の貧困†：1日1.9ドル（米ドル）未満で生活する人々（当時の世界銀行の定義）
```
<!-- 正解はC・C -->
<!-- 1日1.90ドルの「国際貧困ライン」に沿うと、1990年は36%（18億9,500万人）、2015年は10%（7億3,600万人） -->

<!-- 日本での正答率は10%・6% -->

<span style="color:red">
何となくの思い込みで世界を捉えがちである人間の性質を理解した上で，データを一つの切り口として世界を見ることが重要
</span>

---

## オープンデータ

**オープンデータ**：一般に，誰もが利用，複製，再配布，加工などを行えるように，利用条件が明示された形で公開されているデータ．
閲覧可能性だけでなく，再利用可能性が明示されている．

※ ある表がウェブページに掲載されていても利用条件が書かれていない場合には，そのデータを自由に分析したり，再配布したりしてよいとは限らない．
データ分析をする上では**利用条件を確認した上で使う**態度が必要である．
<!-- 
### オープンデータの意義

オープンデータは，行政の透明性向上，科学的検証，市民参加，教育，企業活動など，さまざまな場面で利用される．

たとえば行政統計が機械可読な形で公開されていれば，地域比較，政策評価，都市計画，市場分析などに利用できる．
気象データや交通データであれば，時系列解析，予測，混雑分析，リスク評価などに活用できる．

この意味で，オープンデータは単なる分析素材ではなく，社会の意思決定や知識生成を支える基盤である．
 -->

---

## データ取得で確認すべき事項

分析に用いるデータを取得するときには，少なくとも次の点を確認すること．

- 出典
- ライセンス
- 更新頻度
- 取得時点
- データ形式
- 変数の意味と単位

### 出典

**どの機関，団体，研究プロジェクト，企業などが提供しているデータであるか**を確認する．

出典が不明なデータは正確性や更新性を評価しにくい．  
行政機関の統計，大学研究室の公開データ，民間企業の提供データとでは，作成方法や品質管理の方針が異なる可能性がある．

### ライセンス

ライセンス：そのデータをどこまで利用してよいかを定める条件

特に次の点を確認する．

- 商用利用が可能か
- 改変が許可されているか
- 再配布が可能か
- 出典表示が必要か
- 利用してはいけない用途があるか

### 更新頻度と取得時点

データは常に固定されているとは限らない．  
特に，気象，交通，経済，人口，感染症などのデータは継続的に更新される．

同じプログラムを使ったデータ分析結果であったとしても，データを取得した日時が異なると異なる分析結果が出る可能性がある．
<!-- そのため，データ分析の際には，いつ取得したデータであるかを記録しておくことは重要である． -->

<!-- このとき，分析結果を
$$
R = F(D_t, P)
$$
と表すことができる．
ここで，$D_t$ は時点 $t$ に取得したデータ，$P$ は分析プログラム，$R$ は分析結果である．

同じプログラム $P$ を使っても，取得時点 $t$ が異なれば，入力データ $D_t$ が変わり，結果 $R$ も変わる可能性がある．
したがって，取得日時を記録することは，再現可能性の観点から重要である． -->

### データ形式

同じ内容のデータでも，CSV，JSON，Excel，HTML，PDF など，形式が異なれば処理方法が変わる．
データ形式は**データの構造**を表している．

<!-- データに何が書かれているかだけでなく，どのような構造で保存されているかを理解する必要がある． -->

---

## 機械可読性とデータ形式

### 機械可読性

人間が読みやすい形式と，プログラムが処理しやすい形式は必ずしも一致しない．

**機械可読性**：プログラムによって処理しやすい性質

例）PDF：ファイルに掲載されている表は人間は理解しやすいが，プログラムで集計するには追加の変換が必要になることが多い．

例）CSV・JSON：構造が比較的明確であり，プログラムで扱いやすい．

### 代表的な形式

- CSV：行と列からなる表形式データ．単純で扱いやすい．
- Excel：表形式データだが，複数シート，装飾，結合セルなどを含むことがある場合には人間には読みやすいが機械には読みにくい．
- JSON：キーと値の組を基本とし，リストや入れ子構造を表現できる．
- HTML：ウェブページ表示用の構造をもつ．分析には抽出処理が必要になることがある．
- PDF：文書表示用の形式．人間には読みやすいが，機械処理には向かないことが多い．

### CSVと表データ

CSV：表形式データを扱う基本的な形式の1つ．多くの場合，各行が観測単位，各列が変数を表す．

例）市区町村別人口データ

- 行：各市区町村
- 列：都道府県名，市区町村名，人口，面積など

データ表は，行の集合 $I$ と列の集合 $J$ に対して値を返す写像
$$
X : I \times J \to V
$$
とみなせる．
ここに $V$ は値の集合である．
<!-- この見方をすると，表データは単なる文字の集まりではなく，観測と変数からなる構造体として理解できる． -->
<!-- 
### tidy data の考え方

分析しやすい表データには，次のような原則がある．

- 1つの列は1つの変数に対応する
- 1つの行は1つの観測に対応する
- 1つの表は1種類の観測単位に対応する

この考え方は tidy data と呼ばれる．
取得した直後のデータは，必ずしもこの形になっているとは限らない．
したがって，分析の前には，表の構造を確認し，必要に応じて整理する必要がある．
 -->

### JSONと階層構造

JSON：キーと値（key & value）の組を基本とする形式．
値には文字列，数値，真偽値，リスト，別のオブジェクトなどを含めることができる．
階層的な情報を表す場合に有効である．

例）ある地域の気象情報

```json
{
  "location": "Tokyo",
  "observations": [
    {"date": "2025-04-01", "temp": 18.2, "rain": 0.0},
    {"date": "2025-04-02", "temp": 16.5, "rain": 12.0}
  ]
}
```

- `location`：地点名
- `observations`：観測値のリスト

---

## データの保存と整理

### 生データと加工データ

分析の過程では，取得直後のデータと加工後のデータを区別することが重要である．
生データに変更を直接上書きすると，どこまでが元データで，どこからが自分の加工結果なのか分からなくなる．
この混同を未然に防ぐため，次のようなディレクトリ構成を取ると良い．

```text
project/
├── data/
│   ├── raw/
│   └── processed/
├── src/
└── README.md
```

- `data/raw/`：取得直後のデータ（生データ）
- `data/processed/`：前処理や集計を行った後のデータ（加工データ）
- `src/`：取得や加工に用いるプログラム（ソースファイル）
- `README.md`：出典，取得日，ライセンス，作業手順などの記録

````{note} 演習1
`/User/<ユーザ名>/applied_programming_i/4`のフォルダ直下に次のディレクトリ構成を作成せよ．

```text
4/
├── data/
│   ├── raw/
│   └── processed/
├── src/
└── README.md
```
````
<!-- 
```bash
mkdir -p data/raw
mkdir -p data/processed
mkdir -p src
```
 -->

### メタデータ

**メタデータ**：データを説明するためのデータ．
分析に必要な情報でデータ本体だけでは説明することのできないもの．

メタデータとして次の項目が挙げられる．

- ファイル名
- データ名
- 出典
- 取得日
- 最終更新日
- ライセンス
- 変数の意味
- 単位
- 欠損値の扱い
- 注意点

---

## Git管理とデータ取得

第2回・第3回で学んだ Git は，データ取得と整理にも役立つが，データ分析では何でも Git で管理すればよいわけではない．

### Gitで管理しやすいもの

- 取得用スクリプト
- 前処理スクリプト
- README や取得手順書
- メタデータ
- 小規模なサンプルデータ

### Gitで管理しにくいもの

- サイズの大きいデータ
- 頻繁に更新される取得結果
- 個人情報や機密情報を含むデータ
- ライセンス上，再配布できないデータ

データそのものをすべて Git で管理することではなく，**どのようにデータを取得し，どのように加工したか**を記録することが重要である．
<!-- 
データ取得の操作は，次のように表すことができる．
$$
D = G(u, t, \theta)
$$
ここに

- $u$：取得元
- $t$：取得時点
- $\theta$：取得条件
- $D$：取得されたデータ

である．
取得元，取得時点，取得条件を記録しておけば，後から同様の取得を再実行しやすくなる．
 -->

---

## 実データ

実際に公開されているオープンデータを取得して中身を確認する．
題材として，厚生労働省が公開している「新型コロナウイルス感染症 オープンデータ」のうち，**新規陽性者数の推移（日別）**を用いる．  
このデータは，全国および都道府県別の日次データであり，CSV形式で取得できる．

公開ページ：
```text
https://www.mhlw.go.jp/stf/covid-19/open-data.html
```

CSVファイル：
```text
https://covid19.mhlw.go.jp/public/opendata/newly_confirmed_cases_daily.csv
```
※ アクセスするとファイルのダウンロードが始まる．

利用規約：
```text
https://www.mhlw.go.jp/chosakuken/index.html
```

```{note} 演習2
厚生労働省が公開している「新型コロナウイルス感染症 オープンデータ」のCSVファイルをダウンロードして `/User/<ユーザ名>/applied_programming_i/4/data/raw` に保存せよ．  
また，保存したCSVファイルをExcelで開き，次の点を確認せよ．

- 何行程度のデータなのか
- 日付や数値はどのように保存されているか
- 全国と都道府県の値がどのように並んでいるか
```

````{note} 演習3
厚生労働省ホームページで公開されているコンテンツは，特記がない限り「公共データ利用規約（PDL1.0）」に準拠して利用できる．
利用する際は出典を記載する必要がある．

従って，README.mdに次のようなメタデータを記録すること．

```markdown
- 出典：厚生労働省「新型コロナウイルス感染症 オープンデータ」
- URL：https://www.mhlw.go.jp/stf/covid-19/open-data.html
- 取得日：2026年5月12日
```
````

````{warning} 課題1
README.mdに次の形式に則ってメタデータを記載せよ．
その上で変更したファイルを全てコミットせよ．（コミットする前にファイルを保存できているか確認すること）
なお「〇〇」にはどのようなファイルを入れるフォルダかなどの適切な説明を記載すること．  
作成した`README.md`ファイルはWebClass「第4回課題」問1から提出すること．

```markdown
## ディレクトリ構成

- data/raw：〇〇
- data/processed：〇〇
- src：〇〇

## データ取得記録

- データ名：新規陽性者数の推移（日別）
- 出典：厚生労働省「新型コロナウイルス感染症 オープンデータ」
- URL：https://covid19.mhlw.go.jp/public/opendata/newly_confirmed_cases_daily.csv
- ライセンス：〇〇
- 取得日：2026年5月12日
- データ形式：〇〇
- 保存先：data/raw/newly_confirmed_cases_daily.csv
- メモ：全国および都道府県別の日次データ
```
````
<!-- 
- data/raw：取得直後のデータを保存する
- data/processed：加工後のデータを保存する
- src：取得や加工に用いるプログラムを保存する
 -->

---

## Pythonを用いた実データの取得

Pythonでデータの取得手順をプログラム化すれば，手続きが自動化され再現可能なものにしやすい．

````{note} 演習4：CSVをダウンロードする
`data/raw/newly_confirmed_cases_daily.csv` を削除した上で，
`src/download_covid_data.py` を次の内容で作成し実行せよ．

```python
from urllib.request import urlretrieve

url = "https://covid19.mhlw.go.jp/public/opendata/newly_confirmed_cases_daily.csv"
output_path = "data/raw/newly_confirmed_cases_daily.csv"

urlretrieve(url, output_path)

print("saved:", output_path)
```

実行後，`data/raw/newly_confirmed_cases_daily.csv` が作成されていることを確認せよ．
````

---

## データの中身を眺める

データを取得したら，すぐに分析はせずにまず中身を眺める．
ここでの目的は，厳密な分析ではなくデータの形を理解することである．

最初に確認する項目は次の通りである．

- 行数
- 列名
- 先頭の数行
- 日付の形式
- 数値が文字列として読まれているか
- どの都道府県列が含まれているか

````{warning} 課題2：CSVの概要を確認する
`src/inspect_covid_data.py` を次の内容で作成し，CSVの概要を確認せよ．  
なお，コード内の`<行数>` `<列数>` `<列名>`には適切な変数を挿入すること．

```python
import csv

input_path = "data/raw/newly_confirmed_cases_daily.csv"

with open(input_path, encoding="utf-8-sig") as f: # ファイルを開く
    reader = csv.DictReader(f) # CSVの各行を辞書(dict)として読み込む
    rows = list(reader) # 全行をリスト化する
    fieldnames = reader.fieldnames # CSVのヘッダー行（列名）を取得

print("行数:", <行数>)
print("列数:", <列数>)
print("列名:")
print(<列名>)

print("先頭5行:")
for row in rows[:5]:
    print(row)
```

実行後，次を確認せよ．（確認するだけで良い）

1. 何行あるか
2. 何列あるか
3. `Date`，`ALL`，`Tokyo`，`Saitama` などの列があるか
4. 先頭行の日付はどのような形式か

`src/inspect_covid_data.py`を実行した出力結果をスクリーンショットに取り，WebClass「第4回課題」問2へ提出せよ．
````
<!-- 
print("行数:", len(rows))
print("列数:", len(fieldnames))
print("列名:")
print(fieldnames)
 -->

※ `encoding="utf-8-sig"` は，CSVファイルの先頭にBOM†が付いている場合に対応するための指定である．
列名の先頭に見えない文字が混ざることを防ぐため，公開CSVを扱うときには有用である．
†：BOM (Byte Order Mark) とはどのような文字コードで保存されたテキストかを明示する特殊バイト

---

## まとめ

- オープンデータとは，単に閲覧できるデータではなく，再利用条件が明示されたデータである．
- データ取得では，出典，ライセンス，更新頻度，取得時点，形式を確認する必要がある．
- CSV は表形式，JSON は階層構造を表す代表的な形式である．
- 生データと加工データを分けることで，元データと自分の処理結果を区別できる．
- 実際のオープンデータを取得したら，行数，列名，先頭行，一部の値を確認して，データの形を把握する．
- データ本体だけでなく，README やメタデータを残すことで，後から分析過程を説明しやすくなる．
- Git では，データそのものだけでなく，取得手順と整理過程を管理することが重要である．

次回は API の使い方を扱う．

- ウェブ上のデータ提供サービスから，プログラムを用いてデータを取得する方法を学ぶ．
- 今回扱った出典，形式，ライセンス，取得記録の考え方を，より動的なデータ取得へ拡張する．

### 課題の提出物

WebClass「第4回課題」の問1へ課題1で作成した`README.md`を，問2へ課題2の出力結果を提出せよ．

### 課題の提出期限

<span style="color: red; ">5月12日(火)23:59まで</span>

---

## 自主学習用の発展問題

### 一部の列を取り出して見る

取得したCSVには，全国と都道府県別の列が含まれる．
ここでは，すべての列を使うのではなく，`Date`，`ALL`，`Tokyo`，`Saitama` だけを取り出して表示する．

````{note} 発展演習1：必要な列を取り出す
`src/view_selected_columns.py` を次の内容で作成し，実行せよ．

```python
import csv

input_path = "data/raw/newly_confirmed_cases_daily.csv"
columns = ["Date", "ALL", "Tokyo", "Saitama"]

with open(input_path, encoding="utf-8-sig") as f:
    reader = csv.DictReader(f)
    rows = list(reader)

for row in rows[:10]:
    selected = {column: row[column] for column in columns}
    print(selected)
```

実行後，次を確認せよ．

1. `ALL` は何を表すと考えられるか
2. `Tokyo` と `Saitama` は何を表すと考えられるか
3. 表示された値は数値か，文字列か
````
<!-- 
1. `ALL` は何を表すと考えられるか

`ALL` は，全国（全都道府県）の新規感染者数の合計を表していると考えられる。

例えば：

```python id="f8zj8q"
'ALL': '1'
```

は，
その日の全国の新規感染者数が 1 人であることを表していると考えられる。

2. `Tokyo` と `Saitama` は何を表すと考えられるか

`Tokyo` は東京都，
`Saitama` は埼玉県の新規感染者数を表していると考えられる。

つまり，各都道府県ごとの日別感染者数が記録されている。

3. 表示された値は数値か，文字列か

表示された値は文字列（str型）である。

例えば：

```python id="ib39sz"
type(row["ALL"])
```

を確認すると：

```python id="7up4nt"
<class 'str'>
```

となる。

`csv.DictReader` は CSV の値を基本的に文字列として読み込むためである。

そのため，計算に使う場合は：

```python id="7b6ttf"
count = int(row["ALL"])
```

のように整数へ変換する必要がある。
 -->

CSVを読み込むとき，`csv.DictReader` は各行を辞書として扱う．
ただし，CSVファイルに書かれた値は，基本的には文字列として読み込まれる．
数値計算に使う場合は，`int` や `float` による型変換が必要になる．

### 簡単な集計を行う

ここでは本格的な分析ではなく，データの中身を把握するための簡単な集計を行う．
全国，東京，埼玉の合計値と最大値を確認する．

````{note} 発展演習2：簡単な集計
`src/summarize_covid_data.py` を次の内容で作成し，実行せよ．  
なお4箇所の`〇〇`には適切なコードを挿入すること．

```python
import csv

input_path = "data/raw/newly_confirmed_cases_daily.csv"
target_columns = ["ALL", "Tokyo", "Saitama"]

with open(input_path, encoding="utf-8-sig") as f:
    reader = csv.DictReader(f)
    rows = list(reader)

print("期間:", rows[0]["Date"], "から", rows[-1]["Date"], "まで")

for column in target_columns:
    values = [int(row[column]) for row in rows]
    total = 〇〇
    max_value = 〇〇
    max_index = 〇〇
    max_date = 〇〇

    print(column)
    print("  合計:", total)
    print("  最大:", max_value)
    print("  最大日:", max_date)
```

実行後，次を確認せよ．

1. データの期間はいつからいつまでか
2. `ALL`，`Tokyo`，`Saitama` の最大値はいつ記録されているか
3. 合計値を解釈するときに注意すべきことは何か
````
<!-- 
    total = sum(values)
    max_value = max(values)
    max_index = values.index(max_value)
    max_date = rows[max_index]["Date"]
 -->

この集計は，データの中身を眺めるためのものである．
ここで得た値をそのまま結論として使うのではなく，データの定義や集計方法を確認する必要がある．

### 小さな加工データを作成する

取得した生データには多くの列が含まれる．
ここでは，後の授業で扱いやすいように，一部の列だけを取り出した小さなCSVを作成する．

````{note} 発展演習3：加工済みデータを作成する
`src/extract_covid_columns.py` を次の内容で作成し，`data/processed/covid_selected.csv` を作成せよ．  
なお，`〇〇`には自身の好きな都道府県を選び入力せよ．

```python
import csv

input_path = "data/raw/newly_confirmed_cases_daily.csv"
output_path = "data/processed/covid_selected.csv"
columns = ["Date", "ALL", "Tokyo", "Saitama", "〇〇"]

with open(input_path, encoding="utf-8-sig") as f:
    reader = csv.DictReader(f)
    rows = list(reader)

with open(output_path, "w", encoding="utf-8", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=columns)
    writer.writeheader()

    for row in rows:
        writer.writerow({column: row[column] for column in columns})

print("saved:", output_path)
```

実行後，`data/processed/covid_selected.csv` が作成されていることを確認せよ．
````

<!-- 
````{note} 発展課題1：身近なオープンデータを調べる

自分が関心をもつオープンデータを1つ選び，次の表を埋めよ．

| 項目 | 内容 |
| --- | --- |
| データ名 | ここに記入 |
| 出典機関 | ここに記入 |
| URL | ここに記入 |
| ライセンス | ここに記入 |
| データ形式 | ここに記入 |
| 更新頻度 | ここに記入 |
| 分析してみたい内容 | ここに記入 |

その上で，そのデータが分析に使いやすいかどうかを，形式，変数説明，更新頻度，ライセンスの観点から説明せよ．
````

```{dropdown} 解答例

| 項目 | 内容 |
| --- | --- |
| データ名 | 市区町村別人口データ |
| 出典機関 | 国または自治体の統計提供機関 |
| URL | データが掲載されているページの URL |
| ライセンス | 出典表示を条件に利用可能，など |
| データ形式 | CSV |
| 更新頻度 | 年1回 |
| 分析してみたい内容 | 人口密度と地域差の比較 |

**解説**

分析しやすいデータかどうかは，単に面白い内容かどうかでは決まらない．
CSV などの機械可読な形式であること，変数の意味や単位が説明されていること，更新頻度が明確であること，利用条件が確認できることが重要である．
```

````{note} 発展課題2：raw と processed を説明する

次の問いに答えよ．

1. `data/raw` に保存するべきものは何か．
2. `data/processed` に保存するべきものは何か．
3. 元データを直接上書きしない方がよい理由は何か．
4. 加工後データだけを残すと，どのような問題が起こるか．
````

```{dropdown} 解答例

1. `data/raw` には，取得直後のデータを保存する．できるだけ手を加えず，元の状態を保つ．
2. `data/processed` には，前処理，集計，変換などを行った後のデータを保存する．
3. 元データを直接上書きすると，取得時点の状態に戻れなくなり，加工の影響を検証できなくなる．
4. 加工後データだけでは，どのような処理によって作られたのか，元データと比べて何が変わったのかを確認しにくくなる．

**解説**

データ分析では，結果だけでなく，結果に至る過程を説明できることが重要である．
`raw` と `processed` を分けることは，分析過程を追跡可能にするための基本的な工夫である．
```
 -->
