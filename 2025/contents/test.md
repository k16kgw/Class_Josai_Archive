## テーマ
Git の基本的な使い方

## 到達目標
- Git における作業ツリー，ステージング領域，ローカルリポジトリの違いを説明できる．
- `git status`，`git add`，`git commit`，`git log`，`git diff` を用いて，変更履歴を記録・確認できる．
- 変更を適切な粒度でコミットに分ける意義を説明できる．
- VSCode 上で Git の状態を確認し，端末操作との対応を理解できる．
- 提出物や分析メモを Git 管理する基本的な方法を実行できる．

## 準備学習
第1回の内容を復習し，Python と Git が利用できることを確認しておくこと．また，第1回で作成した作業ディレクトリがある場合は，その場所を確認しておくこと．

## この回の構成（105分の目安）
- 0〜10分　前回の復習と本日の狙い
- 10〜55分　前半：理論
- 55〜95分　後半：演習
- 95〜105分　まとめ，次回案内

## 前回の復習
前回は，授業全体の見通しを確認し，Python 実行環境と Git の導入を行った．また，分析作業ではコードだけでなく，作業過程そのものを記録することが重要であることを学んだ．

第1回では，Git は「変更履歴を残す仕組み」であるという導入までを扱った．本日はその続きとして，実際にどのような単位で変更を記録し，どのように整理していくのかを具体的に学ぶ．

## 本日の狙い
本日の中心課題は，Git の基本操作を単なるコマンド暗記としてではなく，**ファイルの状態を管理する仕組み** として理解することである．

特に，次の3点を重視する．

- 変更されたファイルがどのような状態にあるかを把握する
- 変更を意味のある単位でコミットとして記録する
- VSCode と端末の両方から Git の状態を確認できるようになる

## Gitの基本操作を支える考え方

### Gitはファイルの状態を管理する
第1回では，Git を「履歴を残す仕組み」として導入した．第2回では，それをもう少し具体化して，Git を **ファイルの状態管理システム** として捉える．

ファイルは，編集の過程で常に同じ位置にあるわけではない．Git の視点では，あるファイルは大まかに次のような状態をとる．

- まだ管理対象に入っていない状態
- 編集されているが，次の記録対象にはまだしていない状態
- 次のコミットに含めるものとして確定した状態
- 履歴として記録済みの状態

このように考えると，Git の操作は「ファイルを状態空間の中で遷移させる操作」とみなせる．

### 状態遷移としての理解
あるファイル $f$ の状態集合を
$$
S=\{\text{untracked},\text{modified},\text{staged},\text{committed}\}
$$
とする．

このとき，`git add` は
$$
\text{untracked} \to \text{staged}
\quad \text{または} \quad
\text{modified} \to \text{staged}
$$
という遷移を起こす操作とみなせる．

また，`git commit` は，ステージング領域に置かれた変更を履歴へ確定させる操作である．

このように理解すると，コマンドを個別に覚えるよりも，「今ファイルはどの状態にあり，次にどこへ移すのか」を考えながら操作できるようになる．

## Gitが管理する三つの領域

### 作業ツリー
作業ツリーとは，利用者が実際に編集しているファイル群そのものである．エディタでファイルを書き換えた直後の内容は，まず作業ツリーに存在する．

### ステージング領域
ステージング領域は，次のコミットに含める変更を一時的に登録する場所である．

初学者にとっては，この領域がなぜ必要なのか分かりにくいことが多い．しかし，これは履歴を意味のある単位で整理するために不可欠である．たとえば，同じファイルで「誤字修正」と「新機能追加」を同時に行った場合でも，ステージングによってそれらを別々の記録単位に整理できる．

### ローカルリポジトリ
ローカルリポジトリは，コミットとして確定した履歴を保存する場所である．コミットされた内容は，後から参照・比較できる．

したがって，Git の基本的な流れは
$$
\text{作業ツリー} \longrightarrow \text{ステージング領域} \longrightarrow \text{ローカルリポジトリ}
$$
と整理できる．

## コミットの意味

### コミットは保存ではなく記録である
`git commit` は単なる上書き保存ではない．コミットは，「この時点の変更にはこういう意味がある」と明示して履歴に残す行為である．

したがって，コミットには次の2つの側面がある．

- 内容を固定する
- 変更の意味をメッセージで説明する

このため，コミットメッセージは後から履歴を読んだときに内容が理解できるものである必要がある．

### 良いコミットの粒度
分析作業では，1回のコミットに多くの意味を混ぜない方がよい．

たとえば，次の3つを同時に1つのコミットへ入れるとする．

- 列名の修正
- 欠損値処理の変更
- グラフのタイトル修正

このような履歴は，後で読むと「何を目的とした変更なのか」が分かりにくい．したがって，コミットは **説明可能な単位** で分けるのが望ましい．

## 基本コマンド

### 状態確認
最も基本的で重要なコマンドは `git status` である．

```bash
git status
````

現在，どのファイルが未追跡か，どのファイルが変更済みか，どの変更がステージされているかを確認できる．迷ったときはまず `git status` を実行するのが基本である．

### 変更の追加

ファイルをステージング領域へ追加するには，次を実行する．

```bash
git add ファイル名
```

例として `analysis.py` を追加する場合は次の通りである．

```bash
git add analysis.py
```

### コミット

ステージした内容を履歴へ記録するには，次を実行する．

```bash
git commit -m "コミットメッセージ"
```

たとえば，

```bash
git commit -m "READMEを追加"
```

のように書く．

### 履歴確認

記録した履歴を確認するには次を使う．

```bash
git log
```

簡潔に見る場合は次が便利である．

```bash
git log --oneline
```

### 差分確認

作業ツリーと現在の記録状態との差を確認するには次を使う．

```bash
git diff
```

また，ステージした内容を確認したい場合は次を使う．

```bash
git diff --staged
```

差分確認は，「何をコミットしようとしているか」を見誤らないために重要である．

## `.gitignore` の役割

### すべてを管理すればよいわけではない

Git は便利であるが，生成物やキャッシュまで無差別に記録すると履歴が読みにくくなる．

たとえば Python では，次のようなものは通常管理対象から外す．

```text
__pycache__/
.ipynb_checkpoints/
*.pyc
.DS_Store
```

### 分析作業での注意

データ分析では，生データや中間生成物が大きくなりやすい．また，ライセンスや配布条件のあるデータも存在する．そのため，データ本体を直接コミットするよりも，「どこから取得したか」「どう前処理したか」を管理する方が適切な場合が多い．

この観点からも，`.gitignore` は単なる技術設定ではなく，提出物管理やデータ管理の設計に関わる．

## VSCodeを用いたGitの運用

### なぜVSCodeを併用するのか

Git の本質理解には端末操作が重要であるが，実際の作業ではエディタと Git を連携して使うことも多い．VSCode はその代表例であり，ファイル編集と履歴確認を1つの画面で行いやすい．

授業では，端末操作を基本としつつ，VSCode 上では何がどのように見えているのかも理解しておくとよい．

### Source Controlの見方

VSCode では，左側の Source Control から Git の状態を確認できる．ここには通常，次のような情報が表示される．

* 変更されたファイル
* ステージ済みのファイル
* コミットメッセージ入力欄
* 差分表示

これは本質的には `git status`，`git add`，`git commit`，`git diff` を GUI 上で見やすくしたものに対応している．

### 端末操作との対応

たとえば，VSCode 上の操作はおおむね次のように対応づけられる．

* 変更一覧を見る → `git status`
* ファイル横の `+` を押してステージする → `git add`
* 差分を開く → `git diff` または `git diff --staged`
* メッセージを入力してコミットする → `git commit -m ...`

このような対応を意識しておくと，GUI に頼りすぎず，本質を見失いにくい．

### VSCode利用時の注意

VSCode は便利である一方，ボタン操作だけで Git を使うと，「今何をコミットしようとしているのか」を意識しないまま進めてしまうことがある．

そのため，授業では次の原則を推奨する．

* まず `git status` で状態を確認する
* 必要に応じて VSCode の差分画面で中身を見る
* コミット前に何を記録するかを言語化する
* 不安なときは端末で `git diff` や `git log --oneline` を確認する

## 後半演習の目的

後半では，実際にファイルを作成し，変更を加え，差分を確認し，コミットし，履歴を読むという一連の流れを体験する．さらに，端末操作と VSCode 上の表示がどのように対応しているかを確認する．

## 演習1　リポジトリを作成する

新しい作業用ディレクトリ `applied-programming-lecture2` を作成し，その中で Git リポジトリを初期化せよ．

```bash
mkdir applied-programming-lecture2
cd applied-programming-lecture2
git init
git status
```

### 提出用セル（Markdown）

以下を記述せよ．

* `git init` 実行後に何が起こったか
* `git status` に何が表示されたか
* まだファイルがない段階で，Git は何を管理しているといえるか

## 演習2　READMEを作成して最初のコミットを行う

`README.md` を作成し，授業名，氏名，学籍番号，本日の目標を書け．

```markdown
# 応用プログラミングI

- 氏名：ここに氏名
- 学籍番号：ここに学籍番号

## 第2回の目標
Git の基本操作を理解し，変更を履歴として記録できるようになる．
```

その後，次を実行せよ．

```bash
git status
git add README.md
git status
git commit -m "READMEを追加"
git log --oneline
```

### 提出用セル（Markdown）

以下を記述せよ．

* `git add` の前後で状態表示がどう変わったか
* 最初のコミットはどのような意味を持つか
* `git log --oneline` の出力を見て分かったこと

## 演習3　分析用スクリプトを追加する

`analysis.py` を作成し，次の内容を書け．

```python
print("Applied Programming I")
print("Gitの基本操作を練習する")
```

その後，コミットせよ．

```bash
git status
git add analysis.py
git commit -m "分析用スクリプトを追加"
git log --oneline
```

### 提出用セル（Markdown）

以下を記述せよ．

* `README.md` のコミットと今回のコミットの違い
* 履歴を分けて残す利点
* 現在のリポジトリにはどのようなファイルが管理されているか

## 演習4　変更と差分を確認する

`analysis.py` を次のように書き換えよ．

```python
title = "Applied Programming I"
message = "Gitの基本操作を練習する"

print(title)
print(message)
print("差分を確認してからコミットする")
```

その後，次を実行せよ．

```bash
git status
git diff
```

さらにステージしてから次を実行せよ．

```bash
git add analysis.py
git diff --staged
```

### 提出用セル（Markdown）

以下を説明せよ．

* `git diff` で何が分かったか
* `git diff --staged` で何が分かったか
* 差分を確認してからコミットすることの意義

## 演習5　`.gitignore` を作成する

次の内容で `.gitignore` を作成せよ．

```text
__pycache__/
.ipynb_checkpoints/
*.pyc
.DS_Store
```

その後，コミットせよ．

```bash
git add .gitignore
git commit -m ".gitignoreを追加"
git log --oneline
```

### 提出用セル（Markdown）

以下を記述せよ．

* `.gitignore` は何のために必要か
* Python の演習でどのようなファイルを除外したいか
* データ分析プロジェクトで注意すべき管理対象は何か

## 演習6　VSCodeでGitの状態を確認する

このリポジトリを VSCode で開き，Source Control を確認せよ．その後，`notes.md` を作成し，今日学んだコマンドを3つ以上記録せよ．

例：

```markdown
# 第2回メモ

## 今日学んだコマンド
- git status
- git add
- git commit
- git diff
```

次に，VSCode の Source Control 画面で次を確認せよ．

* どのファイルが変更として表示されるか
* 差分画面でどのように変更箇所が見えるか
* ステージ操作を行うと表示がどう変わるか

その後，端末でも確認せよ．

```bash
git status
git diff
```

最後に，コミットせよ．

```bash
git add notes.md
git commit -m "第2回のメモを追加"
```

### 提出用セル（Markdown）

以下を記述せよ．

* VSCode の Source Control と端末の `git status` はどう対応していたか
* VSCode の差分表示はどのように役立ったか
* GUI と CUI を併用する際に注意したいこと

## 演習7　提出物管理を意識した運用

`report.md` を作成し，次のような提出用下書きを作れ．

```markdown
# 第2回レポート下書き

## 実行した操作
ここに記入

## 気づいたこと
ここに記入

## 次回までの課題
ここに記入
```

このファイルをいったんコミットした後，1か所だけ追記し，さらにもう一度コミットせよ．

### 提出用セル（Markdown）

以下を記述せよ．

* 同じファイルに対して複数回コミットする意味
* 提出物の下書きを Git で管理する利点
* 将来，分析レポートを作成するときにどう役立つか

## 発展事項　コミットメッセージの設計

コミットメッセージは短ければよいわけではなく，後から履歴を読んだときに意味が伝わる必要がある．

たとえば，

* `修正`
* `更新`
* `変更`

のようなメッセージは簡潔ではあるが，情報量が乏しい．一方で，

* `READMEに授業目標を追加`
* `analysis.pyの出力を変数利用に修正`
* `第2回メモを追加`

のようなメッセージは，変更の意図が分かりやすい．

## 発展事項　提出物と履歴の関係

提出時に重要なのは，最終版だけではない．どのような過程でそこに至ったかが履歴に残っていれば，自分で見直すときにも，他者が確認するときにも有益である．

分析結果が誤っていた場合でも，どの時点で何を変更したかが分かれば，原因を特定しやすい．この意味で，Git は提出管理の道具であると同時に，思考過程の記録装置でもある．

## 発展演習　コミット履歴を表にまとめる

これまでに作成したコミット履歴を確認し，次の表をノートにまとめよ．

| 項目                | 内容    |
| ----------------- | ----- |
| コミット数             | ここに記入 |
| 最初のコミット           | ここに記入 |
| 直近のコミット           | ここに記入 |
| 最も分かりやすいコミットメッセージ | ここに記入 |
| 改善したいコミットメッセージ    | ここに記入 |

### 提出用セル（Markdown）

以下を考察せよ．

* 良いコミットメッセージの条件は何か
* どのような単位でコミットを切ると読みやすいか
* 自分の履歴を後から見返したときに困りそうな点は何か

## 本日のまとめ

本日は，Git の基本的な使い方として，ファイルの状態管理，ステージング，コミット，差分確認，履歴確認を学んだ．

重要な点は次の通りである．

* Git では，ファイルは状態を持ち，操作によって遷移する
* 作業ツリー，ステージング領域，ローカルリポジトリを区別して理解することが重要である
* `git status`，`git add`，`git commit`，`git log`，`git diff` は基本操作として特に重要である
* コミットは小さく，意味の明確な単位で行う方がよい
* VSCode の Source Control は便利だが，端末操作との対応を理解して使うことが重要である

## 次回予告

第3回では，Git の運用としてブランチとマージを扱う．履歴が単なる直線ではなく，分岐をもつ構造であることを理解し，複数の作業系列を安全に扱う基礎を学ぶ．
